# Google Gemini API Key — create one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Note: WhatsApp needs no API key. You log in by scanning a QR code on first
# run; the session is then saved locally by LocalAuth (.wwebjs_auth/).
